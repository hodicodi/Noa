import { SxProps } from "@mui/material";

const dialogModal: SxProps = {
  width: "30%",
  mx: "auto",
};

const dialogContent: SxProps = {
  bgcolor: "#47025c",
  color: "#ffffff",
};

const dialogText: SxProps = {
  color: "#ffffff",
};
export default { dialogContent, dialogText, dialogModal };
