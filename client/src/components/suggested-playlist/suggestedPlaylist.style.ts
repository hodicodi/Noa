import { SxProps } from "@mui/material";

const card: SxProps = {
  maxWidth: 345,
  borderRadius: 2,
};

const playlistName: SxProps = {
  color: "#ffffff",
  position: "top",
  backgroundColor: "#000",
};

export default { card, playlistName };
