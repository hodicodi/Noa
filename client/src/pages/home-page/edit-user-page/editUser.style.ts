import { SxProps } from "@mui/material";

const handleEditUserPage: SxProps = {
  display: "flex",
  flexDirection: "column",
  rowGap: 6,
  mt: 4,
  minHeight: "100vh",
  alignItems: "center",
};

export default {handleEditUserPage}